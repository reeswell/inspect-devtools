import { useState } from 'react'
export const SettingsForm = () => { const [saved, setSaved] = useState(false); return <form className="settings-form" onSubmit={event => { event.preventDefault(); setSaved(true) }}><label><span>Workspace name</span><input defaultValue="Source Lab" name="workspace" /></label><label><span>Default branch</span><select name="branch" defaultValue="main"><option>main</option><option>develop</option></select></label><label className="switch-row"><span>Build notifications</span><input type="checkbox" defaultChecked /></label><div className="form-actions"><button type="submit">Save settings</button>{saved && <span role="status">Saved</span>}</div></form> }
